import { LoaderContainer, LoaderIcon } from './styled';
import GlobalStyles from '@styled';

const Loader = () => {
	return (
		<LoaderContainer>
			<GlobalStyles />
			<LoaderIcon />
		</LoaderContainer>
	);
};

export default Loader;
